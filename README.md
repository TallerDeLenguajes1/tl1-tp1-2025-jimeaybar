# tp1 Jimena Aybar
 2) Es conveniente incluir .gitignore porque actúa como un filtro que evita ensuciar el repositorio. Evita subir archivos pesador o innecesarios. Impide que subas por error archivos con contraseña o configuraciones sensibles. Mantiene el historial de Git enfocado solo en cambios de código real. Se debe crear al principio del proyecto ya que solo ignora archivos que no han sido rastreados todavia. El archivo .gitignore es un archivo de texto simple ubicado en la raíz del repositorio.
 Se configura escribiendo patrones de nombres de archivos o carpetas.
 - nombre_archivo.ext: ignora un archivo especifico
 - carpeta/ : ignora totod el contenido de una carpeta.
 - *.log: ignora todos los archivos que terminen en esa extensión.
Para ignorar el archivo ignorado.txt debo primeramente crear la carpeta .gitignore en mi repositorio (touch .gitignore) y luego escribir este comando 'echo ignorado.txt >> .gitignore' 
 3) -Código Misterio: este código en la 1ra función suma el resto(num % 10) de un número ya establecido(que en cada iteración se va dividiendo en 10) mientras esa suma la va multiplicando por 10 antes de sumar el nuevo resto, a esta función la llamé f_resto. La 2da función lo que hace es dividir en 2 el numero obtenido de la 1ra función, la renonmbre f_divide. La 3ra función es similar a la primera ya que en la iteración solo va sumando los restos(num%10) del número sin multiplicar y al salir del while suma el número obtenido de la 2da función y la suma obtenida de la iteración en este función, la llamé f_resto_suma. Cabe aclarar que existe una 4ta función que lo que hace es llamar a las primeras tres funciones. Y en el main sólo se llama a la 4ta función y la salida que se obtiene es el resultado de la 3ra función.
  -En codigo_roto.c al trabajar con printf y scanf hay que incluir la libreria stdio.h, otro error es en la linea 12(scanf) falta el operador de dirección & para pasarle la dirección de memoria. En la linea 17 (int suma = valor1 + valor2) falta ';' al final de la línea. Y por último, en la función duplicar_numero(valor1) se está pasando el valor1 por valor por lo que la función no está modificando el valor1 original, se debe hacer el paso por refencia usando punteros (int *numero).
